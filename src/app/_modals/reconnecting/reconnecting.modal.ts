import { Component } from "@angular/core";

@Component({
    templateUrl: 'reconnecting.modal.html',
    styleUrls: ['reconnecting.modal.scss']
})
export class ReconnectingModal {
    constructor( ) { }
}